*.jld2
*.vtk
*.vti
*.vtu
*.edge
*.ele
*.face
*.node
*.smesh
*.stl
*.png
*.sh

!doc/*.png
!doc/*.img
!data/**/*.jld2
!data/**/*.vti
.DS_Store

# Test suite VTU artifacts (per-stage diagnostics + pipeline exports)
test/output/

# Local experiment / cross-validation scaffolding under scratch/
# (keep only the tracked regression harness measure.jl)
scratch/*
!scratch/measure.jl

# Reference material (quartet / isostuffer sources, papers) — read-only, not part of the package
Literature/
